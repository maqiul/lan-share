[package]
name = "lanshare-mount-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
lanshare-client = { path = "../lanshare-client" }
widestring = "1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"

[build-dependencies]
# 不需要，直接链接 dokan.lib
